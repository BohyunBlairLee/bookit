# 📚 Kakao Books API 설정 가이드

## 문제
BookIt 앱의 책 검색 기능이 작동하지 않는 이유는 **Kakao Books API 키**가 설정되지 않았기 때문입니다.

## 해결 방법

### 1단계: Kakao Developers 계정 생성

1. **Kakao Developers 접속**: https://developers.kakao.com/
2. **로그인** (카카오 계정으로)
3. 계정이 없다면 회원가입

### 2단계: 애플리케이션 생성

1. 상단 메뉴에서 **내 애플리케이션** 클릭
2. **애플리케이션 추가하기** 버튼 클릭
3. 앱 정보 입력:
   - **앱 이름**: BookIt (또는 원하는 이름)
   - **사업자명**: 개인 (또는 회사명)
4. **저장** 클릭

### 3단계: REST API 키 복사

1. 생성된 애플리케이션 선택
2. **앱 키** 섹션에서 **REST API 키** 복사
   - 예: `1234567890abcdef1234567890abcdef`

### 4단계: 프로젝트에 API 키 설정

#### 방법 A: .env 파일 생성 (로컬 개발용)

```bash
# 프로젝트 루트 디렉토리에 .env 파일 생성
cd /home/user/bookit
nano .env
```

`.env` 파일에 다음 내용 추가:
```
KAKAO_API_KEY=복사한_REST_API_키_여기에_붙여넣기
```

저장 후 서버 재시작:
```bash
npm run dev
```

#### 방법 B: 환경 변수 직접 설정

```bash
export KAKAO_API_KEY=복사한_REST_API_키_여기에_붙여넣기
npm run dev
```

### 5단계: 테스트

1. 브라우저에서 앱 열기
2. 검색창에 "82년생 김지영" 입력
3. 검색 결과가 나타나는지 확인

---

## 📋 참고사항

### Kakao Books API 사용량
- **무료**: 하루 30,000건 호출 가능
- 개인 프로젝트에는 충분한 양

### API 키 보안
- ⚠️ `.env` 파일을 Git에 커밋하지 마세요
- `.gitignore`에 `.env` 추가 확인
- 배포 시에는 환경 변수로 설정

### 배포 환경 설정

**Replit:**
```
Secrets 탭 > Add new secret
Key: KAKAO_API_KEY
Value: 복사한_API_키
```

**Vercel/Netlify:**
```
Environment Variables > Add
Name: KAKAO_API_KEY
Value: 복사한_API_키
```

**Heroku:**
```bash
heroku config:set KAKAO_API_KEY=복사한_API_키
```

---

## 🆘 문제 해결

### 검색이 여전히 작동하지 않는 경우

1. **서버 재시작 확인**
   ```bash
   # 서버 중지 (Ctrl + C)
   # 서버 시작
   npm run dev
   ```

2. **API 키 확인**
   ```bash
   echo $KAKAO_API_KEY
   # 또는
   cat .env
   ```

3. **서버 로그 확인**
   - 검색 시 터미널에 "⭐" 이모지와 함께 로그가 출력됨
   - API 키가 정상적으로 로드되었는지 확인

4. **API 키 권한 확인**
   - Kakao Developers > 내 애플리케이션 > 설정
   - 앱 상태가 "활성화"인지 확인

---

## 🚀 빠른 테스트 (임시)

API 키 설정 없이 테스트하려면:
```bash
# 임시로 fallback 데이터만 사용
# bookService.ts에서 fallback 데이터를 확장하면 됩니다
```

---

## 📞 추가 도움

- Kakao Developers 문서: https://developers.kakao.com/docs/latest/ko/daum-search/dev-guide
- 질문/이슈: Kakao Developers 포럼
